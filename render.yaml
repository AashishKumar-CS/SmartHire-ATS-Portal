services:
  - type: web
    name: smarthire-backend
    runtime: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/smarthire-*.jar
    envVars:
      - key: PORT
        value: 8080